name: Integration Tests

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  setup:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Create test-repo directory
        run: mkdir ../test-repo

  test-vue-mess-detector:
    runs-on: ${{ matrix.platform }}
    needs: setup
    defaults:
      run:
        working-directory: ../test-repo

    strategy:
      matrix:
        platform: [ 'macos-latest', 'ubuntu-latest', 'windows-latest' ]
        package-manager: [ npm, yarn, pnpm, bun ]

    name: Test Vue Mess Detector with ${{ matrix.package-manager }} on ${{ matrix.platform }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'yarn'

      - name: Check PNPM
        if: matrix.package-manager == 'pnpm'
        uses: pnpm/action-setup@v4
        with:
          version: '9.9.0'

      - name: Check Yarn
        if: matrix.package-manager == 'yarn'
        run: npm install -g yarn

      - name: Check Bun
        if: matrix.package-manager == 'bun'
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: '1.1.26'

      - name: Create Vue Project
        run: |
          cd ../test-repo
          if [ "${{ matrix.package-manager }}" == "pnpm" ]; then
            yes "my-package" | head -n 1 | pnpm create vue@latest . --default --typescript
          elif [ "${{ matrix.package-manager }}" == "yarn" ]; then
            yes "my-package" | head -n 1 | yarn --cwd . create vue . --default --typescript
          elif [ "${{ matrix.package-manager }}" == "bun" ]; then
            yes "my-package" | head -n 1 | bun create vue . --default --typescript --cwd=. --ignore-scripts
          else
            yes "my-package" | head -n 1 | npm create vue@latest -- . --default --typescript

      - name: Install dependencies
        run: |
          cd ../test-repo
          if [ "${{ matrix.package-manager }}" == "pnpm" ]; then
            pnpm install
          elif [ "${{ matrix.package-manager }}" == "yarn" ]; then
            yarn install
          elif [ "${{ matrix.package-manager }}" == "bun" ]; then
            bun install --ignore-scripts
          else
            npm install

      - name: Run Vue Mess Detector
        id: vmd
        run: |
          cd ../test-repo
          if [ "${{ matrix.package-manager }}" == "pnpm" ]; then
              pnpm add vue-mess-detector -D
              pnpm run vue-mess-detector analyze
          elif [ "${{ matrix.package-manager }}" == "yarn" ]; then
              yarn add vue-mess-detector -D
              yarn run vue-mess-detector analyze
          elif [ "${{ matrix.package-manager }}" == "bun" ]; then
              bun add vue-mess-detector --dev
              bun run vue-mess-detector analyze
          else
              npm install vue-mess-detector --save-dev
              npm run vue-mess-detector analyze
